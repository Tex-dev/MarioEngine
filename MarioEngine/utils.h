#pragma once

#include <iostream>
#include <sstream>
#include <vector>
#include <string>

#include <SDL/SDL.h>
#include <SDL/SDL_image.h>

#define max(x,y)	((x)>(y)?(x):(y))
#define min(x,y)	((x)<(y)?(x):(y))
